{
  "name": "pi-extension-mgr",
  "version": "0.1.0",
  "description": "Enable and disable pi extensions at runtime and via a CLI flag. It does not install or remove extensions.",
  "keywords": [
    "pi-package",
    "pi-extension",
    "extensions",
    "ext-mgr"
  ],
  "type": "module",
  "license": "MIT",
  "files": [
    "index.ts"
  ],
  "main": "index.ts",
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "scripts": {
    "check": "echo 'nothing to check'"
  }
}
